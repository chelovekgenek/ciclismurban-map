import styled from "styled-components"
import { Button } from "components/generic/ui"

export const EmptyBox = styled.div`
  text-align: center;
`

export const ActionWrapper = styled(Button.Group)`
  float: right;
`
